[model]
name = Events
label = Events
hidden = yes

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[children]
model = event
order_by = -date, title

[pagination]
enabled = no

